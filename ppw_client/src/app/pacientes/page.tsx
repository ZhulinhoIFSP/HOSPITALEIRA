'use client'; import { useConsultas } from '@/components/consultas/consulta-provider';
export default function PaginaPacientes() { const { pacientes, loading, error } = useConsultas(); if (loading) return <p>Carregando pacientes...</p>; return <><h1 className="text-3xl font-bold">Pacientes</h1><p className="mt-2 text-slate-600">Catálogo fictício disponível para agendamentos.</p>{error && <p className="mt-4 text-rose-700">{error}</p>}<div className="mt-6 max-w-xl overflow-hidden rounded-xl bg-white shadow-sm"><table className="w-full text-left"><thead className="bg-slate-50"><tr><th className="p-3">ID</th><th className="p-3">Nome</th></tr></thead><tbody>{pacientes.map((paciente) => <tr key={paciente.id} className="border-t"><td className="p-3">#{paciente.id}</td><td className="p-3">{paciente.nome}</td></tr>)}</tbody></table></div></>; }
