'use client'; import { useConsultas } from '@/components/consultas/consulta-provider';
export default function PaginaMedicos() { const { medicos, loading, error } = useConsultas(); if (loading) return <p>Carregando médicos...</p>; return <><h1 className="text-3xl font-bold">Médicos</h1><p className="mt-2 text-slate-600">Catálogo fictício disponível para agendamentos.</p>{error && <p className="mt-4 text-rose-700">{error}</p>}<div className="mt-6 max-w-2xl overflow-hidden rounded-xl bg-white shadow-sm"><table className="w-full text-left"><thead className="bg-slate-50"><tr><th className="p-3">ID</th><th className="p-3">Nome</th><th className="p-3">Especialidade</th></tr></thead><tbody>{medicos.map((medico) => <tr key={medico.id} className="border-t"><td className="p-3">#{medico.id}</td><td className="p-3">{medico.nome}</td><td className="p-3">{medico.especialidade}</td></tr>)}</tbody></table></div></>; }
